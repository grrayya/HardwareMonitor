# HardwareMonitor

HardwareMonitor

A terminal-based hardware telemetry dashboard for Windows/Linux, built with rich and psutil. 
- polls CPU load, CPU temperature, and RAM usage in real time, renders a live-updating table in the terminal, and logs every reading to a local SQLite database for later analysis.



Requirements
Python 3.9+
psutil
rich
